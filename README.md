# p2p-file-transfer
Sync files over local network

## Building
This project uses [nob.h](https://github.com/tsoding/nob.h) for compiling.
Compile the `nob.cpp` file using the available C++ compiler, then run the
`nob` executable.
